# PMPLAST KIOSK - Architecture Overview

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         PMPLAST KIOSK                           │
│                    (Flutter Mobile/Web App)                     │
└─────────────────────────────────────────────────────────────────┘
                              │
                ┌─────────────┼─────────────┐
                ▼             ▼             ▼
           Android           iOS          Chrome
         (Mobile)         (Mobile)       (Web)
```

---

## 🎯 Layered Architecture

```
┌────────────────────────────────────────────┐
│         UI LAYER                           │
│  ┌──────────────────────────────────────┐  │
│  │  Registration Screen                 │  │
│  │  - Email/Password Login              │  │
│  │  - Device Name Input                 │  │
│  │  - GPS Capture & Verify              │  │
│  └──────────────────────────────────────┘  │
│  ┌──────────────────────────────────────┐  │
│  │  Kiosk Screen                        │  │
│  │  - QR Scanner                        │  │
│  │  - Camera Preview                    │  │
│  │  - Status Display                    │  │
│  │  - Feedback Messages                 │  │
│  └──────────────────────────────────────┘  │
└────────────────────────────────────────────┘
                   │
        ┌──────────┴──────────┐
        ▼                     ▼
┌──────────────────┐  ┌──────────────────┐
│  STATE MGMT      │  │  THEME & CONFIG  │
│  (Riverpod)      │  │  (Material 3)    │
│                  │  │                  │
│ • Registration   │  │ • Colors         │
│   Controller     │  │ • Typography     │
│ • Kiosk          │  │ • Spacing        │
│   Controller     │  │ • Shadows: None  │
│ • Sync Service   │  └──────────────────┘
└──────────────────┘
        │
┌───────┴─────────────────────────────────────┐
│         SERVICE LAYER                       │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │  Authentication Service             │   │
│  │  - Supabase Auth                    │   │
│  │  - Sign In/Out                      │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │  Device Service                     │   │
│  │  - Device ID Generation             │   │
│  │  - Secure Storage                   │   │
│  │  - Device Registration              │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │  Attendance Service                 │   │
│  │  - Photo Compression                │   │
│  │  - Photo Upload                     │   │
│  │  - RPC Verification                 │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │  Location Service                   │   │
│  │  - GPS Permission Check             │   │
│  │  - Position Request                 │   │
│  │  - Location Verification            │   │
│  └─────────────────────────────────────┘   │
│                                             │
│  ┌─────────────────────────────────────┐   │
│  │  Sync Service                       │   │
│  │  - Connectivity Monitor             │   │
│  │  - Offline Queue Management         │   │
│  │  - Auto-Sync on Connection          │   │
│  └─────────────────────────────────────┘   │
└────────────────────────────────────────────┘
        │
┌───────┴──────────────────────────────────┐
│         DATA LAYER                       │
│                                          │
│  ┌──────────────────────────────────┐   │
│  │  Repositories & Models           │   │
│  │  - Employee                      │   │
│  │  - Device                        │   │
│  │  - PendingLog (Hive)             │   │
│  └──────────────────────────────────┘   │
│                                          │
│  ┌──────────────────────────────────┐   │
│  │  Providers (Dependency Injection)│   │
│  │  - Supabase Client               │   │
│  │  - Secure Storage                │   │
│  │  - Connectivity Stream           │   │
│  └──────────────────────────────────┘   │
└──────────────────────────────────────────┘
        │
┌───────┴──────────────────────────────────┐
│         EXTERNAL SERVICES                │
│                                          │
│  ┌──────────────────────────────────┐   │
│  │  Supabase Backend                │   │
│  │  - PostgreSQL Database           │   │
│  │  - Storage Bucket                │   │
│  │  - RPC Functions                 │   │
│  │  - Authentication                │   │
│  └──────────────────────────────────┘   │
│                                          │
│  ┌──────────────────────────────────┐   │
│  │  Device Capabilities             │   │
│  │  - Camera                        │   │
│  │  - GPS                           │   │
│  │  - Storage                       │   │
│  │  - Network                       │   │
│  └──────────────────────────────────┘   │
│                                          │
│  ┌──────────────────────────────────┐   │
│  │  Local Storage                   │   │
│  │  - Hive (Offline Logs)           │   │
│  │  - Secure Storage (Credentials)  │   │
│  └──────────────────────────────────┘   │
└──────────────────────────────────────────┘
```

---

## 📊 Data Flow Diagram

### Online Flow
```
Employee Scans QR
    ↓
App Detects QR Code
    ↓
Pause Scanner
    ↓
Capture Photo (Camera)
    ↓
Get GPS Location
    ↓
Check Internet → ONLINE
    ↓
Compress Image (JPG, <200KB)
    ↓
Upload to Supabase Storage
    ↓
Call RPC: log_verified_attendance()
    ├─ Employee ID
    ├─ Device ID
    ├─ Photo URL
    ├─ GPS Coordinates
    └─ Timestamp
    ↓
Server-Side Verification
    ├─ Employee exists?
    ├─ Device registered?
    ├─ GPS within 50m?
    └─ Check-in or Check-out?
    ↓
Insert Attendance Log
    ↓
Return Success Message
    ↓
Show Green Screen: "Welcome, John!"
    ↓
Reset & Resume Scanner (3 sec)
```

### Offline Flow
```
Employee Scans QR
    ↓
App Detects QR Code
    ↓
Pause Scanner
    ↓
Capture Photo (Camera)
    ↓
Get GPS Location
    ↓
Check Internet → OFFLINE
    ↓
Compress Image (JPG, <200KB)
    ↓
Save to Temporary Storage
    ↓
Create PendingLog Object
    ├─ Employee ID
    ├─ Photo Path
    ├─ GPS Coordinates
    └─ Timestamp
    ↓
Save to Hive Database
    ↓
Show Message: "Log saved. Will sync when online."
    ↓
Reset & Resume Scanner (3 sec)
    ↓
    (Later when online...)
    ↓
Connectivity Service Detects Connection
    ↓
For Each Pending Log:
    ├─ Read Photo File
    ├─ Upload Photo
    ├─ Get Photo URL
    ├─ Call RPC with URL
    ├─ Delete Photo File
    └─ Remove from Queue
    ↓
All Synced ✓
```

---

## 🔄 State Management (Riverpod)

```
┌─────────────────────────────────────────────┐
│         RIVERPOD STATE MANAGEMENT           │
└─────────────────────────────────────────────┘

┌─────────────────────────┐
│  Providers              │
├─────────────────────────┤
│ • supabaseClientProvider│
│ • secureStorageProvider │
│ • connectivityProvider  │
│ • isOnlineProvider      │
│ • authServiceProvider   │
│ • deviceServiceProvider │
│ • locationServiceProvider
│ • attendanceServiceProvider
│ • syncServiceProvider   │
└─────────────────────────┘
            │
            ▼
┌─────────────────────────┐
│  StateNotifierProviders │
├─────────────────────────┤
│ • registrationController│
│ • kioskController       │
└─────────────────────────┘
            │
            ▼
┌─────────────────────────┐
│  UI Watches Providers   │
├─────────────────────────┤
│ • Registration Screen   │
│ • Kiosk Screen          │
│ • Connectivity Indicator│
└─────────────────────────┘
```

---

## 🗄️ Database Schema

```
┌──────────────────────┐
│    employees         │
├──────────────────────┤
│ id (PK)              │
│ employee_id (UNIQUE) │
│ full_name            │
│ email                │
│ department           │
│ is_active            │
│ created_at           │
└──────────────────────┘
         ↑
         │ 1:N
         │
┌──────────────────────┐
│  attendance_logs     │
├──────────────────────┤
│ id (PK)              │
│ employee_id (FK)     │
│ device_id (FK)       │
│ check_in_time        │
│ check_in_photo_url   │
│ check_in_location    │
│ check_out_time       │
│ check_out_photo_url  │
│ check_out_location   │
│ date                 │
└──────────────────────┘
         ↑
         │ 1:N
         │
┌──────────────────────┐
│    devices           │
├──────────────────────┤
│ id (PK)              │
│ device_name          │
│ device_unique_id     │
│ is_active            │
│ location (GPS)       │
│ created_at           │
└──────────────────────┘
```

---

## 🔐 Security Architecture

```
┌────────────────────────────────────┐
│      SECURITY LAYERS               │
└────────────────────────────────────┘

Layer 1: CLIENT-SIDE
├─ Secure Storage (Encrypted)
│  └─ Device Unique ID
│  └─ Auth Tokens
│
├─ Permission Checks
│  └─ Camera Access
│  └─ Location Access
│  └─ Storage Access
│
└─ Platform-Specific
   ├─ Android: Biometric (optional)
   └─ iOS: Keychain

         ↓

Layer 2: TRANSPORT
├─ HTTPS/TLS
├─ Certificate Pinning (optional)
└─ API Key Rotation

         ↓

Layer 3: SERVER-SIDE (Supabase)
├─ Authentication
│  └─ Supabase Auth
│
├─ Authorization
│  └─ Row Level Security (RLS)
│
├─ Validation (RPC)
│  ├─ Employee exists
│  ├─ Device registered
│  └─ Location within 50m
│
└─ Encryption
   ├─ Data in transit (TLS)
   └─ Sensitive data at rest
```

---

## 📱 Platform Differences

```
┌───────────────────────────────────────────┐
│         Feature Matrix                    │
├─────────────┬──────────┬────────┬─────────┤
│ Feature     │ Android  │  iOS   │  Web    │
├─────────────┼──────────┼────────┼─────────┤
│ QR Scanner  │    ✅    │   ✅   │   ✅    │
│ Camera      │    ✅    │   ✅   │   ✅    │
│ GPS         │    ✅    │   ✅   │   ✅    │
│ File System │    ✅    │   ✅   │   ❌*   │
│ Offline DB  │    ✅    │   ✅   │   ⚠️**  │
│ Encryption  │    ✅    │   ✅   │   ✅    │
└─────────────┴──────────┴────────┴─────────┘

* Web uses memory (not persistent)
** Browser localStorage with limited size
```

---

## 🔄 Component Interaction

```
┌─────────────────────────┐
│   Kiosk Screen          │
├─────────────────────────┤
│ 1. Display camera feed  │
│ 2. Show QR overlay      │
│ 3. Scan QR code         │
└──────────┬──────────────┘
           │ (triggers)
           ▼
┌─────────────────────────┐
│  Kiosk Controller       │
├─────────────────────────┤
│ 1. Parse employee ID    │
│ 2. Get current position │
│ 3. Capture photo        │
│ 4. Check connectivity   │
└──────────┬──────────────┘
           │ (uses)
           ├─ Attendance Service
           ├─ Location Service
           ├─ Device Service
           └─ Sync Service
           │ (updates)
           ▼
┌─────────────────────────┐
│  Supabase Backend       │
├─────────────────────────┤
│ 1. Call RPC function    │
│ 2. Verify employee      │
│ 3. Check location       │
│ 4. Insert log entry     │
│ 5. Return success msg   │
└──────────┬──────────────┘
           │ (returns)
           ▼
┌─────────────────────────┐
│   Kiosk Screen (Result) │
├─────────────────────────┤
│ 1. Show green/red screen│
│ 2. Display message      │
│ 3. Play success/fail    │
│ 4. Auto-reset after 3s  │
└─────────────────────────┘
```

---

## 🎬 Startup Sequence

```
App Launch
    ↓
Supabase.initialize()
    ├─ Connect to backend
    └─ Load config
    ↓
Hive.initFlutter()
    ├─ Initialize local DB
    ├─ Register adapters
    └─ Open pending_logs box
    ↓
ProviderScope()
    └─ Initialize Riverpod
    ↓
SplashScreen
    ├─ Check device ID stored?
    │   ├─ YES → Show Kiosk
    │   └─ NO → Show Registration
    ↓
Listeners Active
    ├─ Connectivity Stream
    ├─ Location Updates
    └─ Background Sync
```

---

## 🎯 Key Design Decisions

| Decision | Reason |
|----------|--------|
| **Riverpod** | Type-safe state management, easy testing |
| **Hive** | Fast local storage, great for offline |
| **Supabase RPC** | Server-side validation, security |
| **GPS Verification** | Prevents remote check-ins |
| **Photo Compression** | Faster upload, smaller storage |
| **Material 3** | Modern, professional design |
| **No Shadows** | Clean, minimalist aesthetic |
| **Flat Colors** | Better readability |

---

## 📈 Scalability Considerations

```
Current Architecture Supports:
├─ Single office: ✅ Ready
├─ Multiple kioskks: ✅ Ready
├─ Thousands of employees: ✅ PostgreSQL scales
├─ Years of historical data: ✅ Indexed queries
├─ High photo upload rate: ✅ CDN-backed storage
└─ Real-time sync: ✅ Cloud optimized

Future Enhancement Paths:
├─ Admin Dashboard
├─ Reports & Analytics
├─ Multi-office Support
├─ Mobile Admin App
└─ Integration APIs
```

---

This architecture is:
✅ **Scalable** - Grows with your needs  
✅ **Secure** - Multiple layers of security  
✅ **Maintainable** - Clear separation of concerns  
✅ **Testable** - Dependency injection ready  
✅ **Reliable** - Offline support built-in  

---
